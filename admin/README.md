Admin panel code for Maeri / Lumière.

- Move `maeri-admin-panel.html` and related admin assets into this folder.
- You can later split this into:
  - `src/` for JS/TS and components
  - `public/` for static HTML/assets

