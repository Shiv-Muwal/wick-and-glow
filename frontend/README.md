Frontend code for Lumière / Maeri candles.

- Place all user-facing HTML files here (index, shop, product, about, blog, contact, etc.).
- Place shared assets like CSS and JS inside this folder as subfolders (e.g., `css/`, `js/`, `images/`).

