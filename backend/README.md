Backend code for Lumière / Maeri candles.

- Add your server-side code here (Node, PHP, Python, etc.).
- Typical structure (example only):
  - `api/` for REST APIs
  - `models/` for database models
  - `config/` for environment and settings

